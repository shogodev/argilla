
/*   -=========== Popups ===========-   */

// Base

.popup {
  display: none;
  position: absolute;
  background: $body-bg;
  z-index: 333;
}

.popup--close {
  width: 40px;
  height: 40px;
  position: absolute;
  top: 0; right: 0;
  cursor: pointer;
}